kind: Pod
apiVersion: v1
metadata:
  name: multiplepod3
spec:
  containers:
    - name: c00
      image: ubuntu
      command: [ "/bin/bash", "-c","while true; do echo Hello sumeet-singh from ubuntu; sleep 5; done"]

    - name: c01
      image: debian 
      command: [ "/bin/bash", "-c","while true; do echo Hello sumeet-singh from debian; sleep 5; done"]
