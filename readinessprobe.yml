kind: Pod
apiVersion: v1
metadata:
  name: mypod
  
spec:
  containers:
    - name: c001
      image: httpd
      ports:
        - containerPort: 80
  
      readinessProbe:
       initialDelaySeconds: 5
       httpGet:
        path: /
        port: 80
